# Example Files

วางไฟล์ตัวอย่างโปรเจกต์ E3.series (`.e3s`) ไว้ในโฟลเดอร์นี้

หลังจากเพิ่มไฟล์แล้ว ใส่ลิงก์ดาวน์โหลดในหน้าโมดูลที่เกี่ยวข้อง (เช่น [schematic.html](../schematic.html),
[cable.html](../cable.html), [panel.html](../panel.html)) ตรงส่วน "ตัวอย่างฝึกทำ" เช่น:

```html
<a href="examples/circuit-01.e3s">ดาวน์โหลด circuit-01.e3s</a>
```
